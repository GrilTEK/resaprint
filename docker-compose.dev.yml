services:
  db:
    ports:
      - "55432:5432"

  api:
    volumes:
      - ./backend/app:/app/app
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]

  ingest-worker:
    volumes:
      - ./backend/app:/app/app
