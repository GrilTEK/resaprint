fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
greenlet==3.1.1
asyncpg==0.30.0
alembic==1.14.0
pydantic==2.10.3
pydantic-settings==2.7.0
httpx==0.28.1
# passlib 1.7.4 (its last release) probes bcrypt.__about__.__version__
# to detect the backend version; that attribute was removed in bcrypt
# 4.1+, which makes passlib print a harmless "(trapped) error reading
# bcrypt version" warning (hashing/verification still work correctly).
# Pin to the last bcrypt release that still has __about__ to avoid it.
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
itsdangerous==2.2.0
jinja2==3.1.4
python-multipart==0.0.20
lxml==5.3.0
cryptography==44.0.0
